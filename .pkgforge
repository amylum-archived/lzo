name 'lzo'
org 'amylum'

licenses 'COPYING'

source(
  type: 'tar',
  url: "http://www.oberhumer.com/opensource/lzo/download/lzo-2.10.tar.gz"
)

configure_flags(
  prefix: '/usr',
  'enable-shared': nil
)

harden

build do
  configure
  make
  install
end

test do
  # TODO: add tests
end
